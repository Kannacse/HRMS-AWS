#############################################################
# Common Variables
#############################################################

# Git Repository
GITHUB_REPOSITORY="https://github.com/Kannacse/HRMS-AWS.git"
DEFAULT_BRANCH="main"

# Workspace
WORKSPACE="/opt/hrms"

# Docker
DOCKER_SOCKET="/var/run/docker.sock"

DOCKER_COMPOSE_FILE="${WORKSPACE}/HRMS-AWS/Backend/hrms_feb_node/hrms_feb_node/docker-compose.yml"

BACKEND_IMAGE="hrms-backend:latest"

BACKEND_DIRECTORY="${WORKSPACE}/HRMS-AWS/Backend/hrms_feb_node/hrms_feb_node"

BACKEND_DOCKERFILE="${BACKEND_DIRECTORY}/Dockerfile"
# Images
CUSTOM_JENKINS_IMAGE="custom-jenkins:lts"
BACKEND_IMAGE="hrms-backend:latest"

# Docker Build
JENKINS_BUILD_CONTEXT="${WORKSPACE}/HRMS-AWS/DevOps"
DOCKERFILE_JENKINS="${WORKSPACE}/HRMS-AWS/DevOps/Dockerfile.jenkins"

BACKEND_DIRECTORY="${WORKSPACE}/HRMS-AWS/Backend/hrms_feb_node/hrms_feb_node"
BACKEND_DOCKERFILE="${BACKEND_DIRECTORY}/Dockerfile"

# Terraform
TERRAFORM_DIRECTORY="${WORKSPACE}/HRMS-AWS/Infra"

# Kubernetes
KUBERNETES_DIRECTORY="${WORKSPACE}/HRMS-AWS/automation/manifests"

# Monitoring
MONITORING_DIRECTORY="${WORKSPACE}/HRMS-AWS/automation/monitoring-manifests"

# Jenkins
JENKINS_HOME="/var/jenkins_home"
