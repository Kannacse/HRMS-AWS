services:

  app:

    image: hrms-backend:latest

    container_name: hrms-backend

    restart: always

    network_mode: host

    env_file:
      - .env

    volumes:
      - /var/www/hrms_mobile_node/logs:/usr/src/app/logs
